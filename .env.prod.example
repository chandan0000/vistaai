
# vistaai - Production Environment
# Generated by fastapi-fullstack v0.1.18
#
# IMPORTANT: Copy this file to .env.prod before deploying
#   cp .env.prod.example .env.prod
#
# WARNING: Never commit .env.prod to version control!

# === Container Prefix (must be unique per server) ===
CONTAINER_PREFIX=vistaai

# === Domain Configuration ===
DOMAIN=example.com
ACME_EMAIL=admin@example.com

# === PostgreSQL ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=  # Generate: openssl rand -base64 32
POSTGRES_DB=vistaai

# === Redis ===
REDIS_PASSWORD=  # Generate: openssl rand -base64 32

# === JWT Secret ===
SECRET_KEY=59a17b46a3995a0c0f593a919172c84478809db568b8d808ea51e53fe623b6d1  # Generate: openssl rand -hex 32

# === Traefik Dashboard ===
# Generate with: htpasswd -nb admin yourpassword
# Example: admin:$apr1$xyz$hash
TRAEFIK_DASHBOARD_AUTH=

# === Flower Monitoring ===
FLOWER_USER=admin
FLOWER_PASSWORD=  # Generate: openssl rand -base64 16
